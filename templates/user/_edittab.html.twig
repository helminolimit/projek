<h1>Edit User</h1>
    
    {# {{ include('user/_form.html.twig', {'button_label': 'Save Changes'}) }} #}
    <a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>
    <a href="{{ path('app_user_show', {'id': user.id}) }}" class="btn btn-primary">back</a>
    
    {# {{ include('user/_delete_form.html.twig') }} #}
 
