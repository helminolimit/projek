<form method="post" action="{{ path('app_bahagian_delete', {'id': bahagian.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bahagian.id) }}">
    <button class="btn">Delete</button>
</form>
